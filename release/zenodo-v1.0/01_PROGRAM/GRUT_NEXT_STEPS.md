# GRUT_NEXT_STEPS.md — THE PRACTICAL PROGRAM (no ladders)

2026-09-06 · governed by the stopping rule of `GRUT_PROGRAM_FREEZE.md` and the presentation
standard of `GRUT_MODEL_FRAMEWORK.md`. Not another RRT. Not a theoretical ladder.

## NEAR-TERM — consolidation
Finish assembling the existing mathematics into the single presentation begun in
`GRUT_MODEL_FRAMEWORK.md`: fold the S_IF machinery, the Tier-4 kernel, the cosmology
exports, and the sector statuses into one self-contained exposition a physicist outside the
project can evaluate. (This is writing and organization, not new derivation.)

## VERIFICATION — only what the presentation leans on
Re-run/verify only calculations that are (a) central to the presented model AND (b) touched
by known defects or reversals: the tt_worldline successor at a **named prescription** (the
contaminated instrument stays preserved; its 16-file fan-out is flagged); the two
campaign-own derivations awaiting independent verification (the no-internal-T lemma; the
CLPW parity-flip isomorphism). Nothing else re-runs by default.

## COMPARATIVE PHYSICS — sector by sector
For each §5 sector, one explicit GRUT ↔ standard-formulation comparison answering exactly:
*what does GRUT change?* Where the honest answer is "vocabulary only," write that. The
KERNEL-STANDARD scope caveat is the standing template for how to say it.

## THE PREDICTION HUNT — the single most valuable target
Not "find anything interesting." **Find one quantitative consequence that is nontrivial and
not already encoded in the inputs.** The structural barriers (no scale from a class; no
ratio from a cone) make *relations between sectors* that the common architecture enforces
and separate formalisms would not the promising direction — the cross-sector KMS lock is
one such locus, not a forced one (owner correction 2026-09-06: nothing establishes it as
the only place such a consequence could live). Candidates on the record, in order of
nearness — each **a currently identified candidate**, not a guaranteed door: the
Γ_T slot (GRUT's own induced tensor friction at ω ~ H₀ — **`calc/gw_tensor_friction.py`
remains staged and unbuilt**, the one live door with a shared parameter and no GRUT entry);
the USL shape signature (distinguishable-in-shape from DP/CSL — needs the magnitude regime
made precise); the rung8 decoherence bookkeeping against tabletop bounds.

## EXPERIMENTAL BRIDGE
Where a candidate survives, identify whether existing or realistically conceivable
experiments distinguish it: GWTC polarization/luminosity-distance data for Γ_T (a loose
slot-bound |Γ_T| ≲ few×H₀ already exists — a bound on the *shared* slot, not a GRUT
measurement); Gran-Sasso-class underground tests for the USL shape; DESI-class w(z) for the
kernel-family kill condition. Pre-register before computing, per house rule.

## STOP RULE
If, after the comparative pass and the prediction hunt, **no discriminator survives — say
so, in the model document, and stop.** No automatic RRT-2, no rescue mechanism, no new rung.
The freeze's reopening keys are the only doors, and they open from the outside.
