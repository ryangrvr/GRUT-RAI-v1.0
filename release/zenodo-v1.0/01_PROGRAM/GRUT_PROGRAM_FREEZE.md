# GRUT_PROGRAM_FREEZE.md — THE CONSOLIDATED LEDGER AND THE STOPPING RULE

**Date:** 2026-09-06 · **Base:** 69e6495 (== origin/v4) · **Owner-decided freeze**, recorded
by the builder from the committed record only. Register untouched (`beaeb84e8a6f8468…`,
74 nodes). ROOT-0 remains a preserved, uncommitted failed instrument. u3/u4 fences stand.
This document consolidates; it does not create physics, retire register nodes, or overwrite
any prior verdict. Sibling record on `rrt0-phase2`: the RRT-0 arc (74e945f → 510c105).

> **This is a freeze, not a burial.** The program stops climbing because the owner decided it
> stops — the only way this class of ladder ever ends — and the conditions for reopening are
> written below, with the vote assigned to mathematics and nature rather than to the program.

---

## 1 · THE STOPPING RULE (committed governance, owner's words)

> **No new RRT generation, model class, or foundational rung will be created merely because
> the previous one failed.** A new generation is permitted only if an **independently
> motivated** physical or mathematical principle identifies a **specific phenomenon that the
> existing framework cannot represent** — never "we need X because not-X failed."

Corollaries, binding: reverse-engineering the ontology from the desired result is barred; a
supplied structure may never be re-reported as a derived one; the breadcrumb pattern
(failure → missing ingredient → new model → new missing ingredient) is recognized in the
record as partly self-generated — three campaigns independently found the apparatus
manufacturing portions of its own trail (the asymmetric error budget; the frame-entailed
null; the apparatus instantiating the arrow it hunted) — and is therefore not, by itself,
evidence of a deeper rung.

## 2 · THE RECLASSIFICATION

GRUT is henceforth carried as a **proposed constitutive framework with declared primitives**
— the Newtonian standard, not the derive-everything standard: *given these primitives, do the
relationships among them produce correct physics, and eventually a distinguishing
prediction?* Primitives are not a defect; **undeclared or re-billed primitives are.** The
eight-status vocabulary replacing shame-tiering:

`AXIOM` · `EMPIRICAL-INPUT` · `STRUCTURAL-SELECTION` · `DERIVED` · `RECOVERED` ·
`PREDICTED` · `UNRESOLVED` · `SPECULATIVE`

This reclassification changes the standard of success going forward. **It does not change
the current evidence**, which is consolidated below exactly as committed.

## 3 · THE LEDGER

### AXIOMS (the proposed ontology, openly postulated)
- The responsive-medium stance: physical systems relate through history-dependent
  constitutive response (`rung1_ontology_finite_memory` — "a STANCE, explicitly not derived").
- A system/bath decomposition of some form — **with the audit's sharpening: the canonical
  form is not a tensor split (type III₁ forbids it); what survives translation is a one-sided
  inclusion, and the specific partition used by the contract was never declared in D1–D5.**
- The Past Hypothesis (time-orientation input; three guises, one input).
- The Born measure (Bin 1 item 3 — never once entered any campaign's sort; flagged).

### EMPIRICAL-INPUTS
The observed time-asymmetry (retarded response, entropy gradient); the dS-like background
where used; every measured constant consumed.

### STRUCTURAL-SELECTIONS (chosen, priced, honest)
BD-analogue state (D3) · scheme/no-IR-scale (D5) · order of limits (D1) · gauge (D2/D4) ·
the TT projector (`p_tt_ansatz`, interrogated: **CHOSEN, unanimous**) · Lorentz covariance of
the kernel (owner-ruled `assumed`, +1) · the background time-translation flow (`assumed`, +1;
its only named discharge route is Borchers–Wiesbrock, which trades rather than manufactures).

### DERIVED (the short list — everything that actually earned the word)
- **The H¹ = 0 four-channel theorem** at frozen scope, and **Theorem II** (the even-degree
  ladder class — a genuinely new structural identity, premises = two standard identities).
- **The flat-scope kernel**: J ~ (3/1280π)ω⁵ + (13/480π)H²ω³ — s = 5 exactly, convergent,
  Re χ(0) = 3/(2560π²); the registered s = 3 **rejected** by the frozen tolerance.
- **The exact dS constant tail** H²/4π with the conformal gap at m_eff² = 2H² (Δ₋ = 0) —
  the surviving fixed point of every deletion test.
- **The passivity/cone theorems** (channel-diagonal orientation; no amplitude ceiling, no
  ratio pin) — the structural reason no number ever came out.
- **The linear-universe response no-go** (RRT-0 + its correction): Δ = 𝒯ⁿ((E−I)ρ) for ALL
  linear dynamics — unitary, dissipative, non-invertible, Lindblad alike; escaped only by
  nonlinearity. Broader than designed; the program's cleanest theorem-grade product.
- T = T_dS = H/2π uniquely, given the background (Gibbons–Hawking; borrowed, zero freedom).

### RECOVERED — with the mandatory honesty note
The framework reproduces standard QFT/EFT results **because its executed machinery IS
standard machinery on the declared inputs** (KERNEL-STANDARD, scoped). Recovery-by-identity
is compatibility, not correspondence evidence. Nothing here counts toward PREDICTED.

### PREDICTED — **EMPTY**, with the reason on the record
Zero entries. Not for lack of searching (two FOREST hunts, a signature audit, X_FLOOR — all
empty) but for a **derived structural reason**: the framework's commitment is a *class*, a
class has no scale, and the admissible set is an amplitude-homogeneous cone — "every route
from this framework to a number runs outside it." The one discriminator-shaped survivor —
the **w = −1 kernel-class dichotomy** (only an oscillatory pole pair crosses) — is
menu-scope exclusion shared by the whole passive class, explicitly not GRUT-specific.

### UNRESOLVED (genuinely open, fenced, unmoved)
u3 (the selection question — sharpened to three typings; CPR's uniqueness half stands as
unconfronted counterevidence) · u4 (form artifactual, content relocated, fence stands) ·
the ω ≲ H regime (UNASKABLE at current declarations, on four obstructions — not false) ·
the kernel-vs-dressed object identity (**owner fork, still owed**; the dressed reading lands
on the opposite side of the pre-registered boundary) · the half-line/KMS relative datum (the
Final Boss residue — produced by nothing confronted, eliminated by nothing confronted) ·
O2, the interacting graviton zero-mode (undone; decides the fixed point's referent) ·
the RESIDUE and SLOT tests (posed, decidable, unrun).

### SPECULATIVE (permitted to exist, barred from load-bearing)
The responsive-vacuum picture as organizing intuition · self-recording/universal-refresh
(one conditional mathematical home: the Wiesbrock tunnel — direction supplied) ·
black-hole saturation (corpus leans against the BH form) · 1Space (**UNDEFINED** — all seven
candidate definitions failed non-circularity).

## 4 · WHAT THE AUDITS KILLED (preserved, not erased)

The finite-memory/single-pole kernel as a supported structure (its sole support reversed by
a real bug validated only at its blind point; what dS forces is **infinite scale-free
memory** — the negation of the bet) · the u4 "dissolution" (relocation, not derivation) ·
s = 3 (rejected; importing it anywhere is laundering, per the dispatch spec) · G-STRONG
("every structural result needs a hand-supplied region split" — refuted by region-free
counting observables) · both discriminator hunts and the signature audit (empty) · the PAS
address ledgers and the Direction Residue as a certified new type (decomposed: absolute
orientation = gauge; one relative datum survives) · the CPR-alignment route for RRT-0
(reference ABSENT, 6/6 seeds — dissolved, not deferred) · the P1 emergence framing for ALL
linear dynamics (the no-go, above) · and, at the meta-level, three generations of the
program's own instruments (pass-label gates, story-pinned gates, the uncalibrated sorter) —
each caught, stamped, and preserved.

## 5 · REOPENING CONDITIONS (the vote leaves the house)

Any ONE of the following justifies unfreezing, under the stopping rule:
1. **O2 computed**: the interacting graviton zero-mode. Lifted (rate ≈ 0.034H at
   m_eff² = 0.1H²) → the fixed point's referent falls and with it the persistence claim;
   protected → the one surviving derived structure strengthens materially.
2. **The RESIDUE TEST decided**: a derivation of the half-line/KMS alignment from unoriented
   hypotheses (kills the residue framing), or the functorial no-go proved (the within-triple
   half exists, campaign-own, awaiting independent verification).
3. **The SLOT TEST decided**: a rigorous single-patch G_N → 0 limit (CLPW §4.3) — settles
   whether the clock-slot is a second irreducible input.
4. **Nature votes at w = −1**: any observed crossing excludes the entire relaxational class,
   GRUT included, at a stroke — the standing kill condition.
5. **An independently motivated principle** naming a specific phenomenon the framework
   cannot represent — the only door through which an RRT-1/Fork-B or any new rung may enter.

## 6 · WHAT WOULD SHOW THE WHOLE APPROACH WAS WRONG

A self-closing description — any formalism fitting the observed asymmetric phenomena with
zero contentful undischarged inputs — falsifies the consolidated "every closure consumed an
input" record outright. A demonstrated second discharge row anywhere (einselection,
Born-measure sort, CLPW outcome A) breaks the conservation pattern. And if the frozen-
predicate calibration ever shows the consolidated claim deflates paradigm cases the way the
chamber's sorter did, the claim dies as **ill-typed rather than false** — the honest worst end.

## 7 · WHAT SURVIVED, STATED AT FULL STRENGTH

**The method.** *"The transferable asset is the method, not the physics"* — the blind
adjudicator's verdict, earned: pre-registration with hashes before results; two-sided gates;
negative controls with the N1–N10 standard; journal-read, de-pinned instruments; mirror
controls; preserve-the-failure; the stopping rule itself. RRT-0 is its finished
demonstration: an apparatus that said **no** to its own program five consecutive times,
cleanly, cheaply, and on the record — semantic PASS → reducibility PASS → mirror PASS
(bit-exact) → sector UNRESOLVED → reference ABSENT → the no-go broadened to every linear
dynamics. **That instrument — RAI — is the program's existing contribution, independent of
whether the responsive vacuum ever earns a prediction.**

**And one sentence of physics, with its grades welded on:** *on a de Sitter background,
persistence came free; forgetting and direction were always paid for* — the first clause a
controlled derivation conditional on the background; the second an induction over the
examined record, whose universal form is exactly what a one-sided apparatus is least able
to check.

## GOVERNANCE EXIT

Register sha256 unchanged; no node retired or promoted by this freeze; no frozen artifact
edited; ROOT-0 still uncommitted-by-design; the contaminated instrument still preserved
unpatched with its fan-out flagged; A–F unselected; both fences unmoved. The freeze itself
is an **owner decision**, recorded here with the builder as scribe.

## W-0 STATUS — the ladder stops because we decided it stops; what was earned is banked as method, what was killed stays killed, and the reopening keys are hung where the program cannot reach them by itself.
