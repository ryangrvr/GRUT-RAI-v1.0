# The GRUT Signature Audit

*A durable companion to `NO_GO_LEDGER.md` and `POSTULATE_MAP.md`. The standing, honest record of GRUT's **empirical reach**: every observable the program could in principle touch, and what an exhaustive audit found. The headline is not hidden: **at interpretation-level, GRUT is signature-null** — it makes no clean, parameter-free, falsifiable prediction that standard physics does not already make. This is reported straight, because the discipline is the only thing that lets us know it instead of fooling ourselves.*

*Kept consistent, claim-by-claim, with `provenance/claims.json`. "Signature" here means a **positive distinctive observable**; it is distinct from a **falsifiable-direction no-go** (which GRUT does have — see `NO_GO_LEDGER.md`).*

---

## The audit — every candidate differentiator

| Observable | Register | Verdict | Why |
|---|---|---|---|
| **GW dissipation** (Im χ → GW dephasing/attenuation, v_g≠c) | `rung4_love_kk` | **invisible-by-suppression** | real (absent in lossless GR) but ~22–62 orders below LIGO sensitivity; GW170817 speed bound satisfied with 26–66 orders to spare. Planck-suppressed, structural. |
| **Dark-energy w(z)** | `rung7_wz`, `rung7_w2/w3` | **signature-null (sourced = ΛCDM)** | the sourced prediction is w = −1 flat; an evolving w(z) requires an inserted, un-sourced second scale (laundering); no clean DESI match. The wₐ sign is frontier; the no-crossing is a *direction*, not a signature. |
| **Tabletop decoherence** (energy-basis wedge) | `rung8_falsifier` | **invisible-by-suppression (quiet-or-faint)** | qualitatively distinct from Diósi–Penrose/CSL (energy- vs position-basis), but the dominant diagonal coupling samples S(0)=0 (quiet, Γ=0) and the off-diagonal wedge sits 7–47 orders below detectable. Observability would need tuning the amplitude to the current matter-wave bound. |
| **Linear cosmology** (μ, Σ growth) | `mu_linear` | **signature-removing (no-go export)** | μ = 1 = ΛCDM; GRUT *forbids* the μ=4/3 modification its own coefficient naively suggests. This *removes* a would-be signature — deflationary, not predictive. |
| **Founding ζ / info / L₀ screens** | `founding_*`, `info_*`, `l0_*` | **no observable** | each dissolved to a research program / standard-physics relabel / presupposed input — none produced a distinct observable (`info_i3` moot, `founding_h3` analogy-only by rung-8). |
| **Linear-cosmology interior** (μ, Σ, slip across the {shear, bulk} family) | `zeta_interior_family` | **constrained-to-a-computed-window** *(the fourth category, 2026-08-02)* | the family's scalar admixture is **bounded by a measurement** (corrected 2026-08-03: DESI Σ₀ lensing binds — x < ~0.59 central-inputs, loose-upper per the F-MAP fence ⇒ μ−1 ≤ ~0.20; the ISW-cross anchor computed to ~2.0σ retired the old 1/16, and the owed TT-auto calc likely re-tightens) rather than deleted by fiat — the c₀=0 choice is now known CHOSEN, and the parameter space it foreclosed is open, parameterized, and data-pinned. **The null is not softened**: x has no floor, so no detection confirms GRUT and no null refutes it — the family allows up to the edge; it predicts nothing. (Anchor computed 2026-08-03 — `calc/isw_exclusion.py`; the binding channel is now lensing; the TT-auto calc is the owed gate.) |

---

## Audit-critic verdict: **EMPTY**

No admissible, dedicated, parameter-free signature survives. The forward-model harness independently confirmed it operationally: across the responsive-medium kernel family, **no distinctive matching universe is produced without laundering** — the only clean data-consistent vacuum is w = −1 ΛCDM, and reproducing any evolving/DESI-like signal requires an inserted input that the machine flags and refuses. The admissible set is ΛCDM-like; the distinctive-signature set is empty.

**Net:** GRUT's *falsifiable content is not a signature.* It is the **no-go exports** (falsifiable-direction): μ=4/3 excluded, no phantom-divide crossing without an inserted mode, the conformalon cannot unify rung7↔rung9. Its *contributions* are the discipline apparatus, the relocation map (`POSTULATE_MAP.md`), and the honest negatives — **not** a new observable. A theory becomes a *theory* (vs a framework + a method) only if a clean signature appears; this audit records that, as of now, none has.

---

## The one soft spot — black-hole ringdown / QNM

There is exactly **one** observable the audit could not fully close by a dedicated calculation:

- **Black-hole quasinormal modes / ringdown damping.** GRUT's dissipative tidal response (Im χ, `rung4`) is a horizon/tidal dissipation channel that could in principle shift QNM frequencies or ringdown damping times — a place where a *dynamical* (lossy) tidal response differs from GR's conservative one.
- **Flagged status: invisible-by-inheritance, NOT a dedicated calc.** The expectation is that any such shift inherits `rung4`'s structural Planck-suppression (~the same 22+ orders below detectable), so it is *expected* invisible — but this is an **inheritance argument, not a computation.** It is the single observable not given its own forward calculation.
- **Honest disposition:** a soft spot, recorded openly. The likely outcome of a dedicated QNM/ringdown calc is "confirms invisible," but until that calc exists, the audit's EMPTY verdict carries this one explicit caveat. This is the natural next empirical thread *if* the program ever wants to convert "signature-null by inheritance" into "signature-null by calculation."

---

## How to read this audit

- A claimed GRUT "prediction" not on this table → check it is not (a) a relabel of standard physics, (b) a laundered match needing an inserted input, or (c) a no-go *direction* dressed as a positive signature.
- The **EMPTY** verdict is a *result*, not a gap — it is what the responsive-medium premise honestly yields, and it is consistent claim-by-claim with the register.
- **Re-open this audit only** when (a) a dedicated QNM/ringdown calc lands, or (b) a genuinely new observable channel is proposed and survives the same suppression/laundering checks the five above did not.

*This is a standing record. GRUT is signature-null at interpretation-level; the empirical question lives entirely in the bath (whether the spine even has a derivable transport sector — see `POSTULATE_MAP.md`, M1) and, as a distant second, the one un-computed QNM thread.*

---

## 2026-08-02 — External hunt + overseer number-verification (the corrected GW-channel account)

*A four-domain external hunt (GW propagation, cosmology, lab/analogue, high-energy transport; pre-registered A/B/C bar; adversarially refereed) was run, then its load-bearing numbers independently verified by the overseer against primary literature. What banks here is only what survived that verification, with the fences verbatim. Legitimate re-open under clause (b) above: new channels were proposed and survived/failed the same checks.*

**1. The scoped null — CONFIRMS the EMPTY verdict from outside.** No candidate above Grade C **in the explored structure** (the admitted TT-channel / passive-KMS / ΛCDM-at-linear family, across the four searched domains). This is *not* the claim "GRUT is empirically silent" — that is a universal statement no search can establish. The pre-registered bar: A = parameter-free number matching a measurement; B = natural-in-GRUT/awkward-standard; C = mere consistency, worth nothing.

**2. The DESI anti-signature — the live threat, with four attribution fences.** GRUT's sourced prediction is w = −1 flat; the passivity no-go (`rung7_w3`) forbids a single passive relaxor from crossing w = −1. DESI's preferred w₀wₐ trajectory **crosses the phantom divide at z ≈ 0.35–0.5** — exactly the forbidden shape. Fences (verbatim, overseer-verified): **(a)** the preference is DESI **BAO + CMB + SNe** — DESI BAO alone shows no significant preference, and with a fixed r_d anchor the ~3σ does not reproduce; **(b)** the honest headline is **3.1σ (DESI DR2+CMB, arXiv:2503.14738)** — the 4.2σ endpoint rides the contested DESY5 compilation (live Efstathiou-vs-DES systematics dispute; one reanalysis drops it to 0.5–1.5σ); **(c)** Bayesian model comparison on the same data gives only weak-to-moderate evidence; **(d)** the direction is Quintom-B — *phantom in the past, quintessence today* (secondary summaries routinely state it backwards). Status: **pending refutation if the signal consolidates**; GRUT is structurally worse-placed to survive it than ΛCDM+quintessence.

**3. The graviton-mass entry — demoted to vacuous.** The dS-horn m ~ H₀ vs the LVK bound is arithmetically right and evidentially empty: the dispersion observable scales as m², so the induced phase of ANY m ~ H₀-class mass is 10⁻²¹ of threshold (arithmetic on the m~H₀ horn vs the LVK threshold — **the test's reach, NOT a banked GRUT effect size**; see NOT-banked below) — **a test that was never capable of running, not a test passed**. Against the *best* current bound (CMB dipole, 5×10⁻³² eV) the margin is ~1.5 orders, not 10 (the LVK 1.92×10⁻²³ eV, GWTC-4.0, is the weakest bound on the board). And Trivedi–Loeb: any m ~ H₀-class mass has Compton wavelength beyond the Hubble radius — of no observational significance **in principle**.

**4. The corrected GW-channel account — α_M ≠ Im Σ_R (category distinction).** The standard-siren friction term α_M (running Planck mass) comes from a **Hermitian action**: removable by field redefinition, graviton-number-conserving, sign-indefinite (nonlocal models predict amplification, Ξ₀ < 1), achromatic, and **noiseless**. A genuine dissipative Im Σ_R is none of those. They are **slot-degenerate in the mean-field equation only**. Consequences: a detected Ξ₀ ≠ 1 could **never confirm** GRUT, and would bear on GRUT only after a conservative-vs-dissipative decomposition the current parameterization does not perform. The earlier "GRUT's home observable can only ever hurt it" claim **does not survive as stated**.

**5. The Γ_T literature anchor — the verification's find, not the hunt's.** Salcedo–Colas–Dufner–Pajer (JHEP 02(2026)241, arXiv:2507.03103) write down a genuine dissipative tensor friction **Γ_T, explicitly distinct from α_M·H, with a mandatory stochastic source ξ**, in a Schwinger–Keldysh open EFT — the first mainstream parameterization of rung3's object **class** (not GRUT's specific single-pole kernel). Its conservative-vs-dissipative sort matches GRUT's reactive-vs-(Im K_R + N) split, but **the retarded+noise form itself is U1-generic** (standard open-EFT, per the register's own `u1_form_universality`) — **no validation credit accrues for the form**; the genuinely new content is the mainstream application to the *gravitational tensor sector* with Γ_T explicitly distinguished from α_M — and **none of it validates any GRUT value**. Via the slot-degeneracy, current friction measurements transfer as **the first measured bound on this object (the Γ_T slot), transferring loosely to whatever value GRUT eventually computes**: effective Markovian, achromatic |Γ_T| ≲ few × H₀ — **a no-cancellation bound on the shared friction slot, not a decomposed measurement of GRUT's kernel** (α_M is sign-indefinite and slot-degenerate). A bound the audit never had before; it is a *constraint slot*, not a positive.

**The fourth category (2026-08-02, the interior-wave landing):** the audit's verdict classes were *invisible-by-suppression / laundered / signature-null(=ΛCDM) / signature-removing (no-go export) / no-observable*; the interior adds **constrained-to-a-computed-window** — parameter space **bounded by a measurement rather than deleted by fiat**. This does **not** soften the signature-null verdict: GRUT still makes no positive prediction (x has no floor — no detection confirms it, no null refutes it). What changed is *how the space is bounded*.

**Post-interrogation note (2026-08-02):** the p_tt interrogation returned **CHOSEN** — the scalar-sector closure underlying much of this audit's null is an **artifact of a chosen projector, not a derived consequence** (`p_tt_ansatz.boundary_condition`; in medium vocabulary, ζ_vacuum ≡ 0 by fiat). The {shear, bulk} **interior** — ζ_vacuum ≠ 0, short of the excluded trace-only endpoint — is empirically viable and — as of the interior wave — *windowed, not unexplored* (see the fourth-category row): exactly the sector where the observables live. The audit's verdicts stand *for GRUT-as-written*; they are now known to be conditional on that choice.

**NOT banked (uncomputed or category-error; hard fence):** the "self-undermining discovery" scenario as previously worded; **any ≤ 10⁻²¹ figure as a banked number — including reworded as "GRUT's effect"** (**no calculation exists** — grep-verified; a register number with no calc has no falsifier; the 10⁻²¹ appearing in item 3 above is demotion arithmetic about a *test's reach*, not a GRUT effect size, and must not be re-derived into one); any "ET closes ~1.5 orders" claim (one choice inside a 2.5-order forecast spread; the like-for-like ET Blue Book GW-only figure is 0.2–0.3 orders). **Gate to re-admit:** `calc/gw_tensor_friction.py` — compute GRUT's induced cosmological Γ_T at ω ~ H₀ from the admitted single-pole kernel, with the pre-registered test: does the local memory scale connect to the cosmological friction parameter-free, or does the bridge require a new inserted scale (the rung7 failure shape)?
